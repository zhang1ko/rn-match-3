import React, { Component } from 'react';
import{
    StyleSheet
} from 'react-native';

export const gameStyles = StyleSheet.create({
    screen: {
        flexDirection: 'row',
        padding: 10,
        marginLeft: 20,
        fontSize: 20,
        fontWeight: 'bold'
    }
})
